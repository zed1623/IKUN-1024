<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ljh.mapper.ProjectMapper">

    <insert id="insertProject" useGeneratedKeys="true" keyProperty="id" parameterType="com.ljh.pojo.entity.Project">
        INSERT INTO Project (name, owner_login, repo_url, stars, forks, issues, created_at, updated_at, importance_score)
        VALUES (#{name}, #{ownerLogin}, #{repoUrl}, #{stars}, #{forks}, #{issues}, #{createdAt}, #{updatedAt}, #{importanceScore});
    </insert>

</mapper>
